# App de campo (PWA)

Copia servible de `C:\laragon\www\Controla_Supervision`. Sin BD. `API_URL` → `/api` de Controla.

Ver [`docs/PAQUETES-ACCESOS-Y-SUPERVISION.md`](../docs/PAQUETES-ACCESOS-Y-SUPERVISION.md).
